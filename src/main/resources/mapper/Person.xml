<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.czhe.sysmanage.dao.PersonDao" >
    <sql id="BASE_TABLE">
        person
    </sql>

    <sql id="BASE_COLUMN">
        id,name,job,gender,age,salary
    </sql>

    <insert id="insert" parameterType="com.czhe.sysmanage.entity.Person">


    </insert>

    <delete id="delete">

    </delete>
    <!-- 更新用户信息，为空的字段不进行置空 -->
    <update id="updateUser" parameterType="com.czhe.sysmanage.entity.Person">
        UPDATE
        <include refid="BASE_TABLE"/>
        <set>
            <if test="userName != null">
                userName = #{userName, jdbcType=VARCHAR},
            </if>
            <if test="password != null">
                password = #{password, jdbcType=VARCHAR},
            </if>
            <if test="phone != null">
                phone = #{phone, jdbcType=VARCHAR},
            </if>
        </set>
        <where>
            userId = #{userId, jdbcType=INTEGER}
        </where>
    </update>

    <select id="findAll" resultType="com.czhe.sysmanage.entity.Person">

    </select>


</mapper>